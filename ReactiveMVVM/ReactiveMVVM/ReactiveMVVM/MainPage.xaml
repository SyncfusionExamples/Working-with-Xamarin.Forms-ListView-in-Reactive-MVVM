<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ReactiveSample"
             xmlns:sync="clr-namespace:Syncfusion.ListView.XForms;assembly=Syncfusion.SfListView.XForms"
             x:Class="ReactiveSample.MainPage">
    
    <ContentPage.Content>
        <sync:SfListView x:Name="listview" BackgroundColor="AliceBlue" ItemSize="60"  >
            <sync:SfListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <ViewCell.View>
                         <Grid>
                           <StackLayout Orientation="Vertical">
                        <StackLayout Orientation="Horizontal" Padding="20,10">
                            <Label FontAttributes="Bold" Text="{Binding Title}" HorizontalOptions="FillAndExpand"/>
                            <Label Text="{Binding Author}" TextColor="Gray"/>
                        </StackLayout>
                                <StackLayout HeightRequest="3" BackgroundColor="DarkSlateGray"/>
                                </StackLayout>
                            </Grid>

                        </ViewCell.View>
                    </ViewCell>
                </DataTemplate>
            </sync:SfListView.ItemTemplate>
        </sync:SfListView>
    </ContentPage.Content>
</ContentPage>
